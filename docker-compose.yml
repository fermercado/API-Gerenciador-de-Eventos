services:
  meuapp:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - MONGODB_USERNAME
      - MONGODB_PASSWORD
      - JWT_SECRET
      - MONGODB_HOST
      - MONGODB_DATABASE
